# Password required to sign in at /admin (the lead tracker / spreadsheet-CRM replacement).
# Set a strong value before deploying anywhere public.
ADMIN_PASSWORD=change-me

# Optional: override where the SQLite database file lives.
# Defaults to ./data/tradematch.db
# DATABASE_PATH=./data/tradematch.db
